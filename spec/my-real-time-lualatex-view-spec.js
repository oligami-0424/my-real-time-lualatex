'use babel';

import MyRealTimeLualatexView from '../lib/my-real-time-lualatex-view';

describe('MyRealTimeLualatexView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
